---
title: Base Alert Dialog
description: A dialog that requires user response to proceed, built with Base UI components.
component: true
---

<ExamplePreview path="base-alert-dialog/default" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  <CliCommands name="base-alert-dialog" />
</TabsContent>

<TabsContent value="manual">
<Steps>

<Step>
Install the following dependencies:

```bash
npm install @base-ui-components/react
```

</Step>

<Step>
Copy and paste the following code into your project's `components/ui/base-alert-dialog.tsx` file.

<ComponentPreview name="base-alert-dialog" />

</Step>

</Steps>
</TabsContent>

</Tabs>

## Examples

### Destructive

<ExamplePreview path="base-alert-dialog/destructive" />

## API Reference

This component is based on the [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog) primitive and includes the following custom props:

### AlertDialogTrigger

| **Prop**                                                                                                                      | **Type**  | **Default** |
| ----------------------------------------------------------------------------------------------------------------------------- | --------- | ----------- |
| `asChild` <PopoverInfo>When true, the trigger will render as its child element, passing all props to the child.</PopoverInfo> | ~boolean~ | ~false~     |

### AlertDialogClose

| **Prop**                                                                                                                           | **Type**  | **Default** |
| ---------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------- |
| `asChild` <PopoverInfo>When true, the close button will render as its child element, passing all props to the child.</PopoverInfo> | ~boolean~ | ~false~     |

## Credits

- Built with [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog).
