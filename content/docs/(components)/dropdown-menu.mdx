---
title: Dropdown Menu
description: Displays a menu to the user — such as a set of actions or functions — triggered by a button.
component: true
links:
  - label: '@radix-ui/react-dropdown-menu'
    url: https://www.radix-ui.com/primitives/docs/components/dropdown-menu
---

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  <CliCommands name="dropdown-menu" />
</TabsContent>

<TabsContent value="manual">    
<Steps>

### Install ReUI

Refer to the [Installation Guide](/docs/installation) for detailed instructions on setting up ReUI dependencies in your project.

### Install dependencies

```bash
npm i radix-ui
```

### Add component

Copy and paste the following code into your project’s `components/ui/dropdown-menu.tsx` file.

<ComponentPreview name="dropdown-menu" />

</Steps>

</TabsContent>

</Tabs>

## Examples

### Default

<ExamplePreview path="dropdown-menu/default" />

### Checkbox

<ExamplePreview path="dropdown-menu/checkbox" />

### Radio Group

<ExamplePreview path="dropdown-menu/radio-group" />

## API Reference

This component is built using [Radix UI Dropdown Menu](https://www.radix-ui.com/primitives/docs/components/dropdown-menu) primitives. For detailed information, please visit the full API reference.

### DropdownMenu

This component is based on the [DropdownMenu.Root](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#root) primitive. For more details, refer to the original documentation.

### DropdownMenuTrigger

This component is based on the [DropdownMenu.Trigger](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#trigger) primitive. For more details, refer to the original documentation.

### DropdownMenuGroup

This component is based on the [DropdownMenu.Group](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#group) primitive. For more details, refer to the original documentation.

### DropdownMenuPortal

This component is based on the [DropdownMenu.Portal](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#portal) primitive. For more details, refer to the original documentation.

### DropdownMenuSub

This component is based on the [DropdownMenu.Sub](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#sub) primitive. For more details, refer to the original documentation.

### DropdownMenuRadioGroup

This component is based on the [DropdownMenu.RadioGroup](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#radiogroup) primitive. For more details, refer to the original documentation.

### DropdownMenuSubTrigger

This component is based on the [DropdownMenu.SubTrigger](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#subtrigger) primitive and includes the following custom props:

| **Prop**                                                                                         | **Type**  | **Default** |
| ------------------------------------------------------------------------------------------------ | --------- | ----------- |
| `inset` <PopoverInfo>Adds padding to the left to align with menu items with icons.</PopoverInfo> | ~boolean~ | ~false~     |
| `className` <PopoverInfo>Additional class names for styling the submenu trigger.</PopoverInfo>   | ~string~  | <Na/>       |

### DropdownMenuSubContent

This component is based on the [DropdownMenu.SubContent](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#subcontent) primitive and includes the following custom props:

| **Prop**                                                                                       | **Type** | **Default** |
| ---------------------------------------------------------------------------------------------- | -------- | ----------- |
| `className` <PopoverInfo>Additional class names for styling the submenu content.</PopoverInfo> | ~string~ | <Na/>       |

### DropdownMenuContent

This component is based on the [DropdownMenu.Content](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#content) primitive and includes the following custom props:

| **Prop**                                                                               | **Type** | **Default** |
| -------------------------------------------------------------------------------------- | -------- | ----------- |
| `sideOffset` <PopoverInfo>Distance in pixels from the trigger.</PopoverInfo>           | ~number~ | ~4~         |
| `className` <PopoverInfo>Additional class names for styling the content.</PopoverInfo> | ~string~ | <Na/>       |

### DropdownMenuItem

This component is based on the [DropdownMenu.Item](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#item) primitive and includes the following custom props:

| **Prop**                                                                                         | **Type**                                          | **Default** |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------- | ----------- |
| `inset` <PopoverInfo>Adds padding to the left to align with menu items with icons.</PopoverInfo> | ~boolean~                                         | ~false~     |
| `variant` <PopoverInfo>Applies a destructive style to the menu item.</PopoverInfo>               | ~enum~ <PopoverInfo> "destructive" </PopoverInfo> | ~default~   |
| `className` <PopoverInfo>Additional class names for styling the menu item.</PopoverInfo>         | ~string~                                          | <Na/>       |

### DropdownMenuCheckboxItem

This component is based on the [DropdownMenu.CheckboxItem](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#checkboxitem) primitive and includes the following custom props:

| **Prop**                                                                                     | **Type**  | **Default** |
| -------------------------------------------------------------------------------------------- | --------- | ----------- |
| `checked` <PopoverInfo>The controlled checked state of the item.</PopoverInfo>               | ~boolean~ | <Na/>       |
| `className` <PopoverInfo>Additional class names for styling the checkbox item.</PopoverInfo> | ~string~  | <Na/>       |

### DropdownMenuRadioItem

This component is based on the [DropdownMenu.RadioItem](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#radioitem) primitive and includes the following custom props:

| **Prop**                                                                                  | **Type** | **Default** |
| ----------------------------------------------------------------------------------------- | -------- | ----------- |
| `className` <PopoverInfo>Additional class names for styling the radio item.</PopoverInfo> | ~string~ | <Na/>       |

### DropdownMenuLabel

This component is based on the [DropdownMenu.Label](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#label) primitive and includes the following custom props:

| **Prop**                                                                                         | **Type**  | **Default** |
| ------------------------------------------------------------------------------------------------ | --------- | ----------- |
| `inset` <PopoverInfo>Adds padding to the left to align with menu items with icons.</PopoverInfo> | ~boolean~ | ~false~     |
| `className` <PopoverInfo>Additional class names for styling the label.</PopoverInfo>             | ~string~  | <Na/>       |

### DropdownMenuSeparator

This component is based on the [DropdownMenu.Separator](https://www.radix-ui.com/primitives/docs/components/dropdown-menu#separator) primitive and includes the following custom props:

| **Prop**                                                                                 | **Type** | **Default** |
| ---------------------------------------------------------------------------------------- | -------- | ----------- |
| `className` <PopoverInfo>Additional class names for styling the separator.</PopoverInfo> | ~string~ | <Na/>       |

### DropdownMenuShortcut

This component is a custom implementation that displays keyboard shortcuts next to menu items.

| **Prop**                                                                                     | **Type** | **Default** |
| -------------------------------------------------------------------------------------------- | -------- | ----------- |
| `className` <PopoverInfo>Additional class names for styling the shortcut text.</PopoverInfo> | ~string~ | <Na/>       |
