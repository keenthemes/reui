// Keep these lists in sync with content/docs/(root)/changelog.mdx.
export const DOCS_MENU_UPDATES = {
  "data-grid": "Virtualization and row pinning support added",
} as const

export const COMPONENTS_MENU_UPDATES = {
  "button-group": "10 new components added",
  combobox: "6 new components added",
  card: "5 new components added",
  "data-grid":
    "6 new components added with virtualization and row pinning support",
  "dropdown-menu": "5 new components added",
  "input-group": "20 new components added",
} as const

export const BLOCKS_MENU_UPDATES = {
  authentication: "10 new authentication blocks added",
} as const
