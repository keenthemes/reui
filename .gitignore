# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
.turbo
out
build

# fumadocs (generated by `postinstall: fumadocs-mdx`)
.source

# Generated registry build artifacts (rebuilt from source by the
# `components:*` / `registry:*` scripts — see CONTRIBUTING.md).
# The component workspace-package STUBS (package.json, tsconfig.json,
# src/index.ts) are committed so `pnpm install` resolves `workspace:*`;
# only their esbuild `dist/` output is regenerated and ignored.
lib/generated/component-preview-loaders/
packages/registry/bases/*/components/*/dist/

# testing
coverage

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# editors
.idea
.vscode/*
!.vscode/extensions.json
