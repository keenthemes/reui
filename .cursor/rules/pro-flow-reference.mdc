---
description: Follow the shared Pro flow for OSS registry work
alwaysApply: true
---

# Pro Flow Reference

- Read `../pro/FLOW.md` before changing registry sync, shared REUI sources, logos/SVG guidance, or any flow that also affects `pro-sandbox` or `pro/apps/web`.
- `oss/registry-reui` is the free/shared REUI source layer that feeds Pro web; it is not the sellable output tree.
- Preserve the sibling-root assumption so `oss`, `pro`, `pro-sandbox`, and `shadcn` can be synced together.
